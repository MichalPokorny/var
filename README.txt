The following works:
	* Addition
	* Bitwise operations (&, |, ^)
	* Ordering (> >= < <=)
	* Multiplication
	* Division
	* Literal assignment
	* Left and right shift (<< >>); large bits in amount are ignored (as in x86)
